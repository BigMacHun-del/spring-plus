spring:
  datasource:
    url: jdbc:mysql://plus-database.cre282q4sdq2.ap-northeast-2.rds.amazonaws.com:3306/plusdb
    username: admin
    password: 12345678
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate

  cloud:
    aws:
      region:
        static: ap-northeast-2
      parameterstore:
        enabled: false  # 실제 AWS Parameter Store 연동 전까지 false로

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always